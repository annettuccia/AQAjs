let month = "July";
let vacationStatus;

if (month === "July" || month === "August") {
    vacationStatus = "approved";
} else {
    vacationStatus = "denied";
}

console.log(vacationStatus); // approved